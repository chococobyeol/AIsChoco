# HTTP client
httpx>=0.25.0

# Socket.IO (Chzzk: docs say "Socket.IO-client 2.0.3 until supported" -> use 4.x for protocol compat)
python-socketio[asyncio_client]>=4.0.0,<5.0.0
python-engineio>=3.0.0,<4.0.0
aiohttp>=3.9.0

# Env vars
python-dotenv>=1.0.0

# Groq API (OpenAI-compatible)
openai>=1.0.0

# TTS (Qwen3-TTS). For GPU: install PyTorch with CUDA first, then pip install -r requirements.txt
# Stable options: cu118 (11.8), cu126 (12.6), cu128 (12.8). Example: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# Check https://pytorch.org/get-started/locally/ and pick Compute Platform matching your driver.
qwen-tts>=0.1.0
soundfile>=0.12.0
sentencepiece>=0.1.99
sounddevice>=0.4.6

# VTube Studio
pyvts>=0.1.0
# pydantic>=2.0.0
# pyyaml>=6.0
# loguru>=0.7.0
tiktoken>=0.5.0

# Overlay server (chat/speech for OBS browser source)
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
