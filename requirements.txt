# HTTP client
httpx>=0.25.0

# Socket.IO (Chzzk: docs say "Socket.IO-client 2.0.3 until supported" -> use 4.x for protocol compat)
python-socketio[asyncio_client]>=4.0.0,<5.0.0
python-engineio>=3.0.0,<4.0.0
aiohttp>=3.9.0

# Env vars
python-dotenv>=1.0.0

# --- Unused for now; add when implementing TTS / VTube Studio / AI
# pyvts>=0.1.0
# torch>=2.0.0
# torchaudio>=2.0.0
# transformers>=4.35.0
# sentencepiece>=0.1.99
# pydantic>=2.0.0
# pyyaml>=6.0
# loguru>=0.7.0
# tiktoken>=0.5.0
